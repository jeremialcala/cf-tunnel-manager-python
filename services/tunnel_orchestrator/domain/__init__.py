"""Domain layer.

Contains aggregates, entities, value objects, domain events, repository
ports and pure domain services. **No** I/O, no logging side-effects, no
imports from ``application`` or ``infrastructure``.
"""
